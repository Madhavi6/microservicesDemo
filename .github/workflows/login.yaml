name: lOGIN Images

on: 
  push:
    branches:
      - dev--main

jobs:
  login:
    runs-on: ubuntu-latest 
    steps:
      - name: Log in to Azure
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

